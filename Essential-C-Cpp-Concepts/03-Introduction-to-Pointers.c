/* A program can access code and stack section of memory directly but it can't
access heap section of memory(like ssd, hdd), to access this we need pointers.
Also to access outside resources like network, keyboard etc. we nedd pointers.
*/
